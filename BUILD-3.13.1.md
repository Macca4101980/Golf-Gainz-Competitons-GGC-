# GGC Build 3.13.1 — Leave Society + Live Sync hotfix
- Leave Society now creates the updated state explicitly and saves it to Supabase immediately.
- After leaving, the user is switched to another society they still belong to where available.
- Adds a short local-write guard so the Realtime notification generated by the user's own save cannot immediately replace the local change with an older fetch.
- Realtime still converges with a delayed cloud refresh after the guarded write.
- Retains 3.13 live updates and 3.12.2 modal scrolling.
