# GGC Build 3.2 — Fixed package

This is the corrected deployment package.

Fix over the previous 3.2 ZIP:
- Root service-worker cache bumped to `ggc-shell-3.2-fixed`.
- Verified active runtime files contain no `3.13.8` version reference.
- Package version remains `3.2.0`.
- Build 3.2 UX, persistence, course-library and score-stepper changes retained.

Note: historical BUILD markdown files from earlier releases remain in the repository as documentation only and do not affect the running app.
